First of all you need to install all requred modules. To do this just run this command
pip install -r requirements.txt

To work with database run squeal.py file. If you have problems with reading from .csv check file path in reading.py
